# Sidequests Core

Technical overview :
- `SideQuestToken` is an ERC20 with fixed decimals and an initial supply minted at deployment.
- `SideQuestSC` is a challenge escrow that locks rewards, uses a commit-reveal solve flow, and releases tokens to the winner.
- `DistributionDebugContract` is an owner gated distributor for controlled test allocations.

